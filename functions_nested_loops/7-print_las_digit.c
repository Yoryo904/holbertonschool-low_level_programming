#include "main.h"

/**
 * print_last_digit - find and print
 * the last digit of a number.
 * @int: is the type of return it requires
 *
 * Return: the variable lastdigit
 */

int print_last_digit(int)
{
	int lastDigit = n % 10;

	return (lastDigit);
}
