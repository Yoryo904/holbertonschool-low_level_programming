#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print(void)
{
        char *text = "abcdefghiklmnopqrstuvwxyz";
        int i = 0
        for {i = 0; text[i]; i++}
                {
                        print_alphalet_x10()
                }

        return (0);
}
