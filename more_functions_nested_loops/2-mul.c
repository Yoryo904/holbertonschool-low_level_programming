#include "main.h"

/**
 * mul - multiples a and
 * b
 * @a: is the integer that multiplies b
 * @b: is the integer that multiplies a
 *
 * Return: the result of multiplie a
 * and b
 */

int mul(int a, int b)
{
	return (a * b);
}
